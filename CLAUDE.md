# 会話言語

会話は日本語で行ってください。

このプロジェクトの記事（Markdown）はすべて日本語で書かれています。記事内容の言語もこれに合わせて日本語とします。

# 記事間の参照

次回予告は書きません。後続記事の内容を約束する書き方（「次回は〜を導きます」）は、`[[slug]]`でもプレーンテキストでも、記事末尾でも冒頭の導入でも置かないでください。記事順の入れ替えのたびに書き直しが必要になるためです。伏線は張る側ではなく、それを回収する後続記事の側に後方参照として書きます。

前方参照が許されるのは、扱いきれない話題を「本記事では扱いません」と断って先送りする場合だけです。

リンク先が未公開であること自体は問題ありません。公開の順序を考慮し、参照元より先に公開される見込みであれば、未公開記事にも `[[slug]]`（`slugs.tsv` の予約済みslug）でリンクしてかまいません。避けるのは、参照元より後に公開される記事へのリンクです。

# 箇条書きの区切り

`- **項目**：説明` のように、箇条書きの見出しと説明の区切りには全角コロン「：」を使います。`- **項目** — 説明` のようなダッシュは使いません。日本語の本文では `——`（em dash）も使わず、`（）`や句読点に置き換えます。

ただし、コロンの直後が項目を締める数式になる場合は、コロンを置きません。

# レビューの実行

`uv run review`（記事をLLMにレビューさせるツール）は実行しないでください。APIキーとトークン消費が絡むため、準備（`<stem>-prompt.md`の作成など）まで行い、実行コマンドを提示してユーザーの判断に委ねます。手順は [REVIEW.md](REVIEW.md) を参照してください。

レビュー結果の`<stem>.txt`は残す前提のファイルです。指摘を記事に反映した後も削除しないでください（`uv run articles pending`のレビュー済み判定に使われます）。

# ボックス記法

Mathlogでは通常のMarkdownにない定義・定理・証明等の形式を、`&&&type title`〜`&&&`のブロックでサポートしています。

```
&&&def 三角関数
三角関数は...
&&&
```

- 対応しているtype
    - axm：公理
    - def：定義
    - thm：定理
    - cor：定理の系
    - lem：補題
    - conj：予想
    - prop：命題
    - fml：公式
    - prf：証明
    - ex：具体例
    - exc：問題
    - rem：注意
    - 未指定：単純な囲み枠

記事中の`&&&`ブロックは内容の性質に応じて種類を使い分けます。単なる`&&&rem`（注釈）に丸めず、`&&&def`（定義）・`&&&fml`（公式）・`&&&ex`（例）・`&&&prf`（証明）を能動的に使い分けてください。

titleは省略可能で、省略すると見出しは表示されません。形式の中ではMarkdown（引用・箇条書き・文字装飾等）が使えますが、形式の中に形式は基本的に入れられません。

特定の定義・定理を番号付きで参照したい場合は、ラベルを貼って`[[label]]`で参照します。アンカーテキストを変えたい場合は`[アンカーテキスト](#label)`とします。

```
&&&type title [label]
形式の中に書く内容
&&&
```
