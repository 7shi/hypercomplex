# 検証コード

プロジェクトルートで `uv run qi/check/<ファイル名>` により実行します。

- [real-algorithms.py](real-algorithms.py) — PLAN.md §4「実振幅で扱えるアルゴリズムの境界」の検証。有限アーベル群$G$上のフーリエ行列が実になるのが$G$の指数が$2$以下のときに限ること（$(\mathbb Z_2)^n$の指標表＝$H^{\otimes n}$、$\mathbb Z_N$は$N\ge3$で複素）、ドイッチュ・ジョサとベルンシュタイン・ヴァジラニが実の範囲で完結すること、サイモンのアルゴリズムが全段階で実振幅に留まり測定結果が隠れた周期$s$と直交すること、グローバー反復が2枚の実の鏡映（不変平面上で行列式$-1$）の合成として角度$2\theta$の回転になり振幅が$\sin((2k+1)\theta)$となること、パウリのテンソル積が実であることと$Y$の個数が偶数であることの同値、スティーン符号・ショア9量子ビット符号・5量子ビット符号の安定化群が実であること、超密度符号化と量子テレポーテーションが実で完結すること、$\mathrm{QFT}_N$が$N\ge3$で複素であること、位数発見（$a=7,\ N=15,\ r=4$）で複素になるのが逆フーリエ変換の段階のみであり補助レビットによる符号化で同じ確率分布が実振幅で再現されることの検証。
